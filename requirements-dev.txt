python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -U pip
pip install black ruff pre-commit

————————————
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned（对当前用户开放脚本编辑）